{{- if eq .Values.autoscaler "vpa" -}}
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  labels:
    app: globeco-portfolio-service
  name: globeco-portfolio-service-vpa
  namespace: globeco
spec:
  resourcePolicy:
    containerPolicies:
    - containerName: globeco-portfolio-service
      controlledResources:
      - cpu
      - memory
      controlledValues: RequestsOnly
      maxAllowed:
        cpu: 6000m
        memory: 6Gi
      minAllowed:
        cpu: 260m
        memory: 300Mi
  targetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: globeco-portfolio-service
  updatePolicy:
    updateMode: InPlaceOrRecreate
{{- end -}}
