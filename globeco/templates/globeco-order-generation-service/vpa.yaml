{{- if eq .Values.autoscaler "vpa" -}}
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  labels:
    app: globeco-order-generation-service
  name: globeco-order-generation-service-vpa
  namespace: globeco
spec:
  resourcePolicy:
    containerPolicies:
    - containerName: globeco-order-generation-service
      controlledResources:
      - cpu
      - memory
      controlledValues: RequestsOnly
      maxAllowed:
        cpu: 6000m
        memory: 6Gi
      minAllowed:
        cpu: 589m
        memory: 200Mi
  targetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: globeco-order-generation-service
  updatePolicy:
    updateMode: InPlaceOrRecreate
{{- end -}}
