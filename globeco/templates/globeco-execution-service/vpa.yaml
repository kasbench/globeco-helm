{{- if eq .Values.autoscaler "vpa" -}}
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  labels:
    app: globeco-execution-service
  name: globeco-execution-service-vpa
  namespace: globeco
spec:
  resourcePolicy:
    containerPolicies:
    - containerName: globeco-execution-service
      controlledResources:
      - cpu
      - memory
      controlledValues: RequestsAndLimits
      maxAllowed:
        cpu: 6000m
        memory: 6Gi
      minAllowed:
        cpu: 260m
        memory: 200Mi
  targetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: globeco-execution-service
  updatePolicy:
    updateMode: InPlaceOrRecreate
{{- end -}}
